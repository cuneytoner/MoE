# 88 Technical Drawing Selection Notes

This note classifies the first technical drawing outputs for future use.

S3 can be kept as a connection-intent reference, but not as a build detail.

## Selection Notes

- V4 should not be used as technical plan.
- C1 can be kept as "connection intent" only.
- D2 can be kept as "roof screw concept" only.
- Do not show AI labels as factual.
- Do not copy AI bracket geometry.
- Use these outputs to guide future simplified prompts.
- Real usta package still needs manually verified drawings.

## How To Use These Outputs

Use V4 only to explain that a combined usta sheet is desired, not as a usable plan.

Use C1 only to explain that connection detail visibility is desired.

Use D2 only to explain that roof washer/screw layout needs a real plan.

## What To Reject

Reject any generated drawing detail that:

- has garbled labels
- invents dimensions
- shows unclear load paths
- implies wrong bracket placement
- implies exact screw spacing without real verification
- tries to combine too many views in one small image

## Next Step

Use [87-next-simplified-technical-drawing-prompts.md](87-next-simplified-technical-drawing-prompts.md) for the next prompt iteration.
