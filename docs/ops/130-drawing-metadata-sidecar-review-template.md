# Drawing Metadata Sidecar Review Template

Use this template after running deterministic drawing generators with metadata sidecars.

```text
Date/time:
Tool run:
Output directory:

SVG files generated:
JSON sidecars generated:
Does each SVG have matching JSON?

schema_version present?
asset_type present?
asset_name matches SVG?
asset_path exists?
relative_runtime_path correct?
geometry present?
safety_label present?
output card metadata_available true?

Any secret or shell history present?
Git safety result:
Issues found:
```
