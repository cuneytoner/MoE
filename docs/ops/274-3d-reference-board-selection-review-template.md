# 3D Reference Board Selection Review Template

Use this template after reviewing M35.19.

## Review

- Date/time:
- Dashboard URL:
- Active board selected?
- 3D card visible?
- Add to board visible?
- Add disabled without active board?
- Add uses `/items/3d` endpoint?
- Stored item `asset_type` is `3d_model`?
- Stored path is runtime-relative?
- Metadata fallback works for missing artifacts?
- Duplicate add shows `Already in board.`?
- 3D board item avoids generic metadata fetch?
- Existing generic output-card board add still works?
- Board validator accepts 3D item?
- Board export works with 3D item?
- Stale 3D reference does not break board load?
- No asset copy/move/delete/rename action?
- No generation/Blender/Docker/shell action?
- Source-only check result:
- Binary safety result:
- UI build result:
- Issues found:
- Git safety result:
