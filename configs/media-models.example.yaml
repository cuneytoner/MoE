image:
  enabled: false
  real_generation_enabled: false
  default_engine: disabled
  selected_track: flux-schnell-comfyui-placeholder
  allowed_engines:
    - disabled
    - comfyui-placeholder
    - diffusers-placeholder
    - comfyui
  tracks:
    flux_schnell_comfyui:
      enabled: false
      engine: comfyui
      requires:
        - main_flux_model
        - clip_l
        - t5xxl
        - vae
      notes: "Planning only. Do not download in 26.1-pre."

    sdxl_comfyui:
      enabled: false
      engine: comfyui
      requires:
        - sdxl_checkpoint
        - vae_optional
      notes: "Fallback planning only."
  models:
    - id: flux-schnell-placeholder
      enabled: false
      engine: comfyui-placeholder
      path: /home/cuneyt/MoE_Models_Backup/flux-placeholder
      notes: Placeholder only. Do not download in M26.0.

    - id: sdxl-placeholder
      enabled: false
      engine: diffusers-placeholder
      path: /home/cuneyt/MoE_Models_Backup/sdxl-placeholder
      notes: Placeholder only. Do not download in M26.0.

video:
  default_engine: cogvideo-placeholder
  models:
    - id: cogvideo-placeholder
      enabled: false

3d:
  default_engine: blender-placeholder
  models: []

rigging:
  default_engine: blender-placeholder

animation:
  default_engine: blender-placeholder
