import numpy as np

INTENT_VECTORS = {
    "architect": np.random.rand(32).tolist(),
    "code": np.random.rand(32).tolist(),
    "review": np.random.rand(32).tolist(),
    "fast": np.random.rand(32).tolist(),
}